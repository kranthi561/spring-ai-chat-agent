meta {
  name: Send message (agent + tools + RAG)
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/chat/sessions/{{sessionId}}/messages
  body: json
  auth: inherit
}

body:json {
  {
    "content": "What tools do you have? Summarize any retrieved knowledge if relevant."
  }
}
